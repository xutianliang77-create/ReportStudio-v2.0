# ReportStudio-v2.0

Utilities for authoring an OpenClaw skill package.

## What this project provides
- A typed model for skill metadata and examples.
- A renderer that generates a `SKILL.md` document.
- A helper that packages multiple files for skill scaffolding.

## Quick test
```bash
pytest -q
```
